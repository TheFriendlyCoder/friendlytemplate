/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package friendlytemplate.app;

import friendlytemplate.list.LinkedList;

import static friendlytemplate.utilities.StringUtils.join;
import static friendlytemplate.utilities.StringUtils.split;
import static friendlytemplate.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
